---
description: 页面设计规范 · 猫总确认 · 2026-05-27 v3
globs:
  - frontend/src/routes/**/*.svelte
  - frontend/src/lib/components/**/*.svelte
---

# 丸子配音 V3 · 页面设计规范 v3

## 核心原则

1. **卡片化**：整个页面内容区必须使用卡片隔离，不能用线条作为主要分隔方式。
2. **间距统一**：页面四周固定 15px，内部卡片间距固定 10px（`var(--spacing-sm)`）。
3. **Flex Column**：页面容器默认 `display:flex; flex-direction:column;`，仪表盘 / 主内容区用 `flex:1` 吸收剩余空间。
4. **单屏适配**：页面内容必须控制在一屏以内，不允许页面级滚动；放不下的内容使用 Modal、分页、折叠、详情弹窗。
5. **控件规范**：页面内所有下拉框、按钮、输入框、选择框使用统一高度体系。

## 布局结构

- 页面容器：`flex:1; min-height:0; display:flex; flex-direction:column; padding:15px; gap:var(--spacing-sm); overflow:hidden;`
- 仪表盘 / 主内容区：`flex:1; min-height:0; display:grid/flex; gap:var(--spacing-sm); overflow:hidden;`
- 四周留空：15px 不可变，不再使用 `clamp(8px, 1.2vw, 15px)`
- 卡片间距：`var(--spacing-sm) = 10px`
- 修改单卡片不得影响其他卡片位置
- 禁止页面级 `overflow-y:auto` / `overflow:scroll`
- 列表内容放不下时，优先分页；确需滚动的局部列表必须有明确高度、可见渐隐/分页提示，并不得撑破页面

## 卡片样式

```css
background: var(--color-bg-elevated);
border: 1px solid var(--color-border-secondary);
border-radius: var(--border-radius-lg);
```

## 控件高度标准

| 控件类型 | 高度 | CSS 变量 |
|---|---|---|
| 主按钮 / 常规按钮 | 36px | `var(--control-height-sm)` |
| 输入框 / 下拉框 / 选择框 | 36px | `var(--control-height-sm)` |
| 紧凑图标按钮 / 分页按钮 | 28px | `var(--control-height-xs)` |
| 大型主操作 | 40px | `var(--control-height)` |

## 按钮样式

- **主按钮**：实心 primary 色 + 白字 + border-radius(10px)
- **次按钮**：透明底 + border(--color-border) + hover 变主色
- **图标按钮**：26px × 26px + border + hover 变主色
- 操作按钮始终可见（不用 hover 才显示）

## 列表/表格

- 首列左对齐，其余列居中
- 行高 min-height: 38px，可 flex:1 均分
- 每页 6 行 + 翻页组件
- 缩略图：首字符 + 引擎色渐变背景（30px × 30px）
- 徽标：pill 圆角 (border-radius-pill)
- 行 hover 用 var(--color-hover-bg)

## 禁止事项

- 配音区颜色不能动
- 用户面不出现 IndexTTS / qwen / 阿里云 / 仙宫云
- 不加仅 toast "开发中"的死按钮
- 不加 overflow-y: auto
- 不用 border-right / border-left 做面板分割（改用卡片 + gap）
