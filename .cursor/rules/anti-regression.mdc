---
description: 禁止回退已有代码功能 · 防止 AI 大规模删除/简化
globs: 
alwaysApply: true
---

# 反回退铁律（Anti-Regression Directive）

## 绝对禁止

1. **禁止删除已有功能代码替换为简化版本**
   - 不得将组件调用（如 `<Switch>`, `<Slider>`, `<Modal>`）替换为原生 HTML 元素
   - 不得将动态数据获取（API 调用）替换为静态假数据
   - 不得删除已实现的交互功能（如拼音选择器、引擎健康检测）

2. **禁止大规模删减**
   - 单次修改中，删除行数不得超过新增行数的 3 倍（除非用户明确要求删除）
   - 不得在"重构"或"简化"的名义下删除功能性代码

3. **禁止整文件覆写**
   - 修改文件时必须使用 StrReplace 精准替换，**禁止** Write 整文件覆写已有页面
   - 仅在创建全新文件时使用 Write

## 修改前必须

1. **先读后写**：修改任何文件前，必须先 Read 理解现有代码结构
2. **保留上下文**：StrReplace 的 old_string 必须包含足够上下文（至少 3-5 行），确保精准定位
3. **增量修改**：只改需要改的部分，不触碰无关代码

## 新增功能时

- 在现有代码基础上**追加**，不重写整个文件
- 保留所有已有 import、组件引用、样式定义
- 保留所有已有事件处理器、状态变量、API 调用

## 违规判定

以下行为视为严重违规：
- `Switch` 组件 → `<input type="checkbox">` 
- `Slider` 组件 → `<input type="range">`
- `ttsApi.checkHealth()` → 静态数据
- 删除整个 `.svelte` 组件文件
- 删除 `pinyin-pro` 等已集成的第三方库调用
